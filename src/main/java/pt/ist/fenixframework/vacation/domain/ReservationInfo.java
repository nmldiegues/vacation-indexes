package pt.ist.fenixframework.vacation.domain;

public class ReservationInfo extends ReservationInfo_Base {
    
    public ReservationInfo(int type, int id, int price) {
	super();
	setType(type);
	setId(id);
	setPrice(price);
    }
    
}
